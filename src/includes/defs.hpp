#pragma once
namespace Mer
{
    using type_code_index = int;
    using type_len_t = int;

    using PosPtr = std::shared_ptr<size_t>;

    using int_default = long long;
    using real_default = double;
    using byte_default = unsigned char;
}